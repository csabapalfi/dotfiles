SKIP="script README.md"

for dotfile in *
do
  if [[ ! *$dotfile* == $SKIP ]]; then
    ln -sf $(pwd)/$dotfile ~/.$dotfile
  fi
done

ln -sf ~/.sublime ~/Library/Application\ Support/Sublime\ Text\ 2/Packages/User/Preferences.sublime-settings